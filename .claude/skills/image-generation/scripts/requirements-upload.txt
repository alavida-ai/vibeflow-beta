# Cloud Upload Utility Dependencies
# Install with: pip install -r requirements-upload.txt

# Core dependencies for cloud storage
boto3>=1.34.0              # AWS S3 and S3-compatible services (R2)
google-cloud-storage>=2.14.0  # Google Cloud Storage
Pillow>=10.0.0             # Image validation and processing
tenacity>=8.2.3            # Retry logic with exponential backoff

# Optional dependencies for enhanced functionality
# Install with: pip install -r requirements-upload.txt -r requirements-upload-optional.txt
